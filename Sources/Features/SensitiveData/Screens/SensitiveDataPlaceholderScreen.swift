import SwiftUI

struct SensitiveDataPlaceholderScreen: View {
    var body: some View {
        CommandCenterPlaceholderScreen(
            title: "Sensitive Data",
            description: "Profile-scoped sensitive data controls and stored protected values will appear here."
        )
    }
}
