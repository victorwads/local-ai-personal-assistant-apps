import SwiftUI

struct IssuesPlaceholderScreen: View {
    var body: some View {
        CommandCenterPlaceholderScreen(
            title: "Issues",
            description: "Profile-scoped issues will appear here, including active work, status, and resolution history."
        )
    }
}
