import SwiftUI

struct MCPToolsPlaceholderScreen: View {
    var body: some View {
        CommandCenterPlaceholderScreen(
            title: "Tools",
            description: "MCP tool definitions, availability, and execution inspection will appear here."
        )
    }
}
