import SwiftUI

struct WhatsAppWebYAMLDebugPlaceholderScreen: View {
    var body: some View {
        CommandCenterPlaceholderScreen(
            title: "Web YAML Debug",
            description: "Web selector YAML, extraction results, active flows, and element matching diagnostics will appear here."
        )
    }
}
