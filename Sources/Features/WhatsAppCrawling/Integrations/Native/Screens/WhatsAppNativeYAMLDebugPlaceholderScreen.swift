import SwiftUI

struct WhatsAppNativeYAMLDebugPlaceholderScreen: View {
    var body: some View {
        CommandCenterPlaceholderScreen(
            title: "Native YAML Debug",
            description: "Native accessibility YAML, extraction results, active flows, and element matching diagnostics will appear here."
        )
    }
}
