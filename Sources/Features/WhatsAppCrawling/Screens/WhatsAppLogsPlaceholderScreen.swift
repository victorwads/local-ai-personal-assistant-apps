import SwiftUI

struct WhatsAppLogsPlaceholderScreen: View {
    var body: some View {
        CommandCenterPlaceholderScreen(
            title: "Logs",
            description: "WhatsApp crawling and integration logs will appear here for runtime inspection."
        )
    }
}
