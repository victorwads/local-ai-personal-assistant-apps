import SwiftUI

struct ChatsPlaceholderScreen: View {
    var body: some View {
        CommandCenterPlaceholderScreen(
            title: "Chats",
            description: "WhatsApp chats and synced message history will appear here once the chat workspace is implemented."
        )
    }
}
