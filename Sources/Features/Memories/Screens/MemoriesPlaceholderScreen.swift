import SwiftUI

struct MemoriesPlaceholderScreen: View {
    var body: some View {
        CommandCenterPlaceholderScreen(
            title: "Memories",
            description: "Saved profile memories will appear here for review, search, and lifecycle management."
        )
    }
}
